# SimpleScalar 캐시 알고리즘 수정 보고서

## 1. 수정 개요

SimpleScalar의 캐시 교체 정책에 **MRU (Most Recently Used)** 알고리즘을 추가했습니다.

## 2. 수정 내용

### 2.1 MRU 정책이란?

- **LRU (Least Recently Used)**: 가장 오래 사용되지 않은 블록을 교체
- **MRU (Most Recently Used)**: 가장 최근에 사용된 블록을 교체 (LRU의 반대)

### 2.2 왜 MRU를 추가했나?

1. **순차 접근 패턴에서 유리**: 배열을 순차적으로 한 번만 읽는 경우, 방금 읽은 데이터는 다시 사용되지 않으므로 MRU가 더 효율적
2. **구현이 간단**: 기존 LRU 구조를 활용하여 교체 대상만 변경
3. **성능 비교 용이**: LRU와 정반대 동작으로 워크로드별 성능 차이를 명확히 분석 가능

### 2.3 수정된 파일

- `simplesim-3.0/cache.h`: MRU enum 추가
- `simplesim-3.0/cache.c`: MRU 교체 로직 구현

## 3. 사용 방법

### 3.1 컴파일

```bash
cd simplesim-3.0
make clean
make
```

### 3.2 실행 예시

```bash
# LRU 정책 사용 (기존)
./sim-cache -cache:dl1 dl1:256:32:4:l test-program

# MRU 정책 사용 (새로 추가)
./sim-cache -cache:dl1 dl1:256:32:4:m test-program
```

**옵션 설명**: `dl1:256:32:4:m`

- 256 sets
- 32 byte block size
- 4-way associative
- **m**: MRU 정책 (l=LRU, r=Random, f=FIFO, m=MRU)

## 4. 실험 아이디어

### 4.1 비교 실험

동일한 벤치마크를 LRU와 MRU로 각각 실행하여 비교:

```bash
# LRU로 실행
./sim-cache -cache:dl1 dl1:256:32:4:l benchmark > lru_result.txt

# MRU로 실행
./sim-cache -cache:dl1 dl1:256:32:4:m benchmark > mru_result.txt
```

### 4.2 분석 지표

- **Hit Rate**: 캐시 적중률
- **Miss Rate**: 캐시 미스율
- **Replacement Count**: 교체 횟수
- **IPC (Instructions Per Cycle)**: 성능 지표

### 4.3 예상 결과

- **순차 접근 워크로드**: MRU가 LRU보다 나을 수 있음
- **지역성이 강한 워크로드**: LRU가 MRU보다 우수
- **랜덤 접근**: 두 정책 모두 비슷한 성능

## 5. 보고서 작성 팁

### 5.1 포함할 내용

1. **수정 동기**: 왜 MRU를 추가했는지
2. **구현 상세**: 코드 변경 부분 설명
3. **실험 설계**: 어떤 벤치마크를 사용했는지
4. **결과 분석**: 그래프와 표로 성능 비교
5. **결론**: 어떤 상황에서 어떤 정책이 유리한지

### 5.2 그래프 예시

- Miss Rate 비교 (LRU vs MRU)
- Hit Rate 비교
- 실행 시간 비교

## 6. 추가 수정 아이디어 (선택사항)

더 복잡한 수정을 원한다면:

1. **Adaptive 정책**: 런타임에 LRU/MRU를 자동 선택
2. **Frequency-based**: 접근 빈도를 고려한 교체
3. **Hybrid 정책**: LRU와 MRU를 조합

## 7. 참고사항

- 원본 코드는 백업되어 있음
- 수정은 최소한으로 유지하여 안정성 확보
- 기존 LRU, Random, FIFO 정책은 그대로 동작
